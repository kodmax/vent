define(['overscore'], function(_) {
	'use strict';

	var addClass = function (name) {
		var add = (name + '').split(/\s+/);
		
		_.each(this, function (node) {
			var current = node.className.split(/\s+/);
			
		});
	};
	
	return addClass;
});